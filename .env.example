# Path del file di log JSONL dell'agente (default: logs/agentic.log)
AGENTIC_LOG_PATH=logs/agentic.log

# Chiave API per chiamare il tool `llm`
OPENAI_API_KEY=tuo_codice_qui_segreto

# Modello di default per il tool `llm` (opzionale)
OPENAI_MODEL=gpt-4o-mini
